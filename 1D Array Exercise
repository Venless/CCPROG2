#include <stdio.h>

int main ()
{
	int x;
	int y = 0;
	printf("Enter a Number: ");
	
	scanf("%d", &x);
	printf("\n");
	
	int nArray [x];
	
	do
	{
		nArray[y] = y;
		printf("nArray[%d] = %d\n", y, nArray[y]);
		y++;
	}
	while (x != y);
	
	return 0;
}
